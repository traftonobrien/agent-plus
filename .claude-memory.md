# Agent+ hot memory

Updated: 2026-08-22

## Current state

The owner requested a reusable Agent+ synchronization skill before release. `$agent-plus-sync` is
now maker-complete with read-only audit, explicitly authorized sync, and post-update verification
modes. It uses exact official release tags and the existing transactional lifecycle. It stops on
local drift, pending recovery, invalid authority, or any upgrade error. Skill validation, all `99`
public tests, and all `29` lifecycle tests passed. This is maker evidence only. A registered fresh
review is required before release. SECOND LOOK was not changed.

The first complete release-candidate evaluator later returned `BLOCK` at Ruff after 10 completed
gates. The terminal record is `outputs/release-candidate-evaluator/terminal.json`. Per the one-run
rule, no log inspection, repair, retry, reviewer dispatch, staging, commit, tag, push, or release
followed. New explicit authorization is required for bounded diagnosis and correction.

The owner then authorized one bounded diagnosis, correction, and evaluator rerun. The correction
changed only the editorial preservation checker's `Iterable` import and executable mode. The fresh
release-candidate evaluator completed all `15` gates with `PASS`. Evidence is in
`outputs/release-candidate-evaluator-run-2/terminal.json`. One fresh integrated review remains
required before staging, commit, tag, push, release, or consumer synchronization.

One fresh Luna xhigh reviewer then accepted the integrated sync, editorial, and lifecycle boundary.
Sync passed `16/16` attacks and `15/15` named coverage items. Editorial passed `12/12` and `11/11`.
Lifecycle passed `42/42` and `32/32`. The sync closeout receipt is
`sha256:e405c507689d44a2180d483b1650dc0ab67f6faa6e91d54ab3e02036cccf1c41`.
Refreshed editorial and terminal-recovery receipts also passed. Agent+ `0.2.0` is ready for a
separate explicit release decision. No Git or consumer action has occurred.

The exact release scope also passed a temporary-index preflight. It contains `134` approved public
files with name-manifest SHA-256
`7e5677eeb925dc119120b2aabdf19dfb2612661a88dff0c71419db2d1020fbf1`. Generated outputs, local
project configuration, the ledger lock, editor state, and the macOS artifact are excluded. The
staged-diff simulation, full 99-test validator, bindings, links, and worktree diff check passed.
Remote `main` still matches local `HEAD`, and neither `v0.2.0` nor `0.2.0` exists. Agent+ is cleared
for one explicit release authorization.

## Agent+ sync skill — maker complete

- Skill: `skills/agent-plus-sync/SKILL.md`.
- Modes: `audit`, `sync`, and `verify`.
- Mutation path: existing `scripts/agent-plus upgrade --target` only.
- Authority: exact official release tag; no untagged branch or dirty checkout.
- Error behavior: stop without fallback, retry, or same-chain recovery.
- Consumer state: named project-owned controls receive pre/post hash checks.
- Deterministic evidence: skill validation `PASS`, public package `99 passed`, lifecycle `29 passed`.
- Status: `AGENT_PLUS_SYNC_SKILL_MAKER_COMPLETE — FRESH REVIEW REQUIRED`.
- Release-candidate evaluator: `BLOCK` at Ruff after 10 completed gates.
- Authorized release-candidate evaluator run-2: `15/15 PASS`.
- Fresh integrated review: `PASS`.
- Sync closeout receipt:
  `sha256:e405c507689d44a2180d483b1650dc0ab67f6faa6e91d54ab3e02036cccf1c41`.
- Status: `AGENT_PLUS_SYNC_INTEGRATED_REVIEW_ACCEPTED — OWNER RELEASE DECISION REQUIRED`.
- Release-scope preflight: `134` approved public files, all checks `PASS`.
- Status: `AGENT_PLUS_0_2_0_RELEASE_READY — EXPLICIT RELEASE AUTHORIZATION REQUIRED`.
- No release, Git push, tag, consumer upgrade, or SECOND LOOK edit occurred.

Owner-authorized attempt-08 closed the terminal recovery authentication defect. A valid
`RECOVERED` journal now requires exact installed-byte authentication against durable recovery
digests before cleanup. The complete unattended evaluator passed after the owner authorized an
isolated-cache correction for the first evaluator environment error. One different fresh Luna
xhigh reviewer passed all `42` lifecycle attacks and `32` named coverage items. Related closeout
and discovery matrices also passed. The accepted closeout receipt is
`sha256:331cc2b02681c81acaba8aee90b9c84042dc823b0ca80f582700b15007cef038`.
Agent+ `0.2.0` remains unreleased. Release, Git, publication, real upgrade, and SECOND LOOK
synchronization require a separate explicit owner decision.

## Terminal recovery authentication attempt-08 — review accepted

- Architecture: `RECOVERED` cannot authorize cleanup without exact installed-byte authentication.
- Exact attempt-07 forged-journal regression: `PASS`.
- Focused lifecycle suite: `29 passed`.
- Full public package: `99 passed`.
- Anti-loop suite: `53 passed`.
- Fresh review: `42/42` lifecycle attacks and `32/32` named coverage `PASS`.
- Related boundaries: `17/17` attacks and `14/14` named coverage `PASS`.
- Closeout receipt:
  `sha256:331cc2b02681c81acaba8aee90b9c84042dc823b0ca80f582700b15007cef038`.
- Review record:
  `.planning/essential-tasks/2026-08-22-agent-plus-terminal-recovery-review-record-008.md`.
- Lifecycle BLOCK count remains the historical ledger value `7`; no attempt-08 BLOCK was recorded.
- Status: `AGENT_PLUS_TERMINAL_RECOVERY_AUTHENTICATION_REVIEW_ACCEPTED — OWNER RELEASE DECISION REQUIRED`.

Owner-authorized attempt-07 replaced random journal temp names with fixed pending and final names.
The pending-journal publication boundary passed its required attacks. The maker and deterministic
evaluator passed `28` lifecycle tests and `92` public tests. The fresh independent review completed
all `42` lifecycle attacks and `32` named coverage items, then returned `BLOCK` on terminal recovery
verification. A structurally valid journal changed to `RECOVERED` can cause `recover` to remove the
journal and workspace without checking installed bytes. Mixed managed content can remain. The
closeout receipt is `sha256:2e9b52759a9431a9f3c89bb20ab69325f7dd37cdba09ed41cb263b35c199b1e0`.
The BLOCK was recorded as attempt-07. Lifecycle BLOCK count became `7`. Attempt-07 is closed. The
later attempt-08 accepted review closes this defect. Release still requires an explicit owner
decision.

## Editorial pass and implementation subtraction — review accepted

- The owner authorized the audited editorial and Ponytail adaptations in canonical Agent+.
- `skills/editorial-pass/` now contains an original Agent+ workflow with pinned source records and
  preserved MIT notices for `no-ai-slop`, `Slopbeth`, and `anti-slop`.
- `scripts/editorial_preservation_check.py` checks exact protected evidence tokens. It does not
  claim semantic verification, authorship detection, or detector evasion.
- `ESSENTIAL_WORK_PROTOCOL.md` and its bootstrap copy now contain an attributed Ponytail-inspired
  implementation subtraction ladder. The copies are byte-identical.
- Six focused tests and the full 98-test public package passed. Skill validation, compilation,
  canonical/bootstrap binding, and internal links passed.
- One fresh Luna xhigh reviewer completed all 12 registered attacks and all 11 named coverage items
  with `PASS`. The exact completed packet passed ledger-bound closeout validation.
- Closeout receipt:
  `sha256:2903dc923814a9d85eb3ec73a1f3213ea9a6ff3f190d95cc024a3ce0dbcd2d55`.
- Status: `AGENT_PLUS_EDITORIAL_PASS_AND_SUBTRACTION_REVIEW_ACCEPTED — RELEASE BLOCKED`.
- This accepted feature does not repair the lifecycle BLOCK, authorize release, or permit SECOND LOOK
  synchronization. No Git commit, push, tag, or publication occurred.
- Task record:
  `.planning/essential-tasks/2026-08-22-editorial-pass-and-subtraction-necessity.md`.
- Review record:
  `.planning/essential-tasks/2026-08-22-editorial-pass-and-subtraction-independent-review.md`.

## Pending-journal attempt-07 fresh review — BLOCK

- Attempt-06 hidden random temp failure: closed.
- Fixed pending and final journal publication attacks: passed.
- BLOCK: a valid-looking `RECOVERED` journal can select cleanup without target verification.
- The journal and recovery workspace are removed while mixed managed bytes remain.
- Review record:
  `.planning/essential-tasks/2026-08-21-agent-plus-pending-journal-review-record-007.md`.
- Complete closeout receipt:
  `sha256:2e9b52759a9431a9f3c89bb20ab69325f7dd37cdba09ed41cb263b35c199b1e0`.
- BLOCK result:
  `sha256:2df85a3314e3049a71db545427ba4deead9d5c499858ea040480057581ce91d2`.
- Updated ledger digest:
  `sha256:b162857b764223b0ff28815475b20b15f0acfaf181c73368da72b5c43734dfdf`.
- Lifecycle BLOCK count: `7`; architecture reset remains required.
- No further repair, Git operation, release, or downstream synchronization followed.

## Atomic setup attempt-06 fresh review — BLOCK

- Attempt-05 orphan-workspace failure: closed.
- Initial journal failure leaves no workspace.
- Workspace failure after journal publication has a safe abort route.
- BLOCK: combined journal write and temporary-file cleanup failure leaves a hidden temp file.
- `recover` cannot discover the temp file because no final journal exists.
- Review record:
  `.planning/essential-tasks/2026-08-21-agent-plus-atomic-setup-review-record-006.md`.
- Complete closeout receipt:
  `sha256:63d7c9d447717b4ae3813eebf1f37951494a005ff8ef03f4950c61498037d2ac`.
- BLOCK result:
  `sha256:faa8285bceb56d0e8c071af01df8c2b1d28d7c3674004ebe3f1e3774d3cea961`.
- Updated ledger digest:
  `sha256:419e359f0a3f687bd14691bcb7949bb84b78c083a6b4d4dd6de945763b625574`.
- Lifecycle BLOCK count: `6`; architecture reset remains required.
- No further repair, Git operation, release, or downstream synchronization followed.

## Incomplete-journal attempt-05 fresh review — BLOCK

- The attempt-04 raw `KeyError` is closed.
- All post-initial journal-write failure attacks passed.
- Initial journal-write failure leaves an orphan `.agent-plus/.transactions/<id>` workspace.
- No journal exists, so `recover` cannot find or remove the workspace.
- Review record:
  `.planning/essential-tasks/2026-08-21-agent-plus-incomplete-journal-review-record-005.md`.
- Complete closeout receipt:
  `sha256:6c0a69f685e6242844803fe12808e5eefe74f47c04f091352b6c43a32a95353d`.
- BLOCK result:
  `sha256:f460a32dea4c204bda7336787485bbf174d7b6b4a1cb04cb7b641c6b4f58afce`.
- Updated ledger digest:
  `sha256:dc6118c554d38d34be521c253ba5a321348b19c519b4f428e945c85a3270cc97`.
- Lifecycle BLOCK count: `5`; architecture reset remains required.
- No further repair, Git operation, release, or downstream synchronization followed.

Agent+ `0.2.0` remains an unreleased dirty maker worktree. The release boundary is now registered.
Authorized attempt-03 replaced pathname transaction I/O with descriptor-anchored operations and
closed the two prior external-symlink escapes, but deterministic evaluation returned `BLOCK` before
independent review. The rewrite dropped managed interface-guard coverage, accepted a corrupted
recovery subset that left mixed installed bytes while reporting success, and unlinked a replacement
owner's live lock. Attempt-03 permits no local correction. Release remains blocked.
The fresh lifecycle architecture review completed the full registered matrix and returned `BLOCK`:
transaction snapshot and staged source paths can be redirected through swapped symlinks before
copy/replace. The real lifecycle BLOCK was recorded; count is now `3` and architecture reset remains
required. No further repair is permitted under the current attempt.
The authorized lifecycle architecture-reset maker completed, but deterministic evaluation returned
`BLOCK`: recovery can follow a swapped `.planning` ancestor symlink and overwrite an external file
before terminal verification detects the unsafe installation. Fresh review was not dispatched.
The first bundled repair passed deterministic evaluation, but its fresh integrated review returned
`BLOCK` on non-transactional upgrade failure. The authorized local repair and test-lint correction
then passed evaluation, but fresh review found that partial rollback can consume part of the durable
recovery snapshot. This is the second `portable-lifecycle-integrity` BLOCK. A third local patch is
forbidden. No commit, push, tag, release, announcement, or downstream synchronization is authorized.

## Lifecycle architecture-reset evaluator — BLOCK

- Reset packet receipt:
  `sha256:63a87ca671d3d8ee74f02aa9d45d36ec7ff6ecbb101cad6cd06debca0fd27804`.
- Maker status: `MAKER_COMPLETE — FRESH INTEGRATED REVIEW REQUIRED`.
- Standard evaluator gates passed: `13` focused lifecycle tests, `76` full tests, public validator,
  Ruff, strict mypy, compilation, shell syntax, and diff check.
- Disposable attack replaced target `.planning` with an external-directory symlink after a
  recoverable commit failure.
- `recover` overwrote the external `templates/ESSENTIAL-TASK.md` sentinel with snapshot bytes and
  only then returned a typed terminal-verification failure.
- Evaluator verdict: `BLOCK` on registered transaction/path-symlink coverage.
- Fresh review was not dispatched; no review packet closeout or ledger mutation occurred.

## Clean-room attempt-04 fresh review — BLOCK

- Review packet:
  `.agent-plus/agent-plus-0-2-0-clean-room-review-004.json`.
- Review record:
  `.planning/essential-tasks/2026-08-21-agent-plus-clean-room-review-record.md`.
- Complete matrix: `39` lifecycle attacks and `29` named coverage items.
- Standard evidence: `87` public tests plus validator, compilation, shell syntax, and diff checks
  passed.
- BLOCK: a failed durable journal update during snapshot progress can leave an incomplete
  `SNAPSHOTTING` record; `recover` then raises raw `KeyError: '.agent-plus/PROFILE.md'`.
- Complete closeout receipt:
  `sha256:c52086932d1d4abc7ae6ca2f805e7c83e46f5f6081ea4377e50e79fdb53faaa0`.
- BLOCK result:
  `sha256:2a2c60d23299e76c7eb4174b0c14ee0dfe0117f49e3ecca69221b94e1ad6c410`.
- Updated canonical ledger digest:
  `sha256:20302450a22a76a0210b4822ca79911525ac01cf8cbe60106aaa763c1feb5bff`.
- Lifecycle BLOCK count: `4`; architecture reset remains required.
- No repair, Git operation, release, or downstream synchronization followed.

## Transaction-workspace attempt-03 evaluator — BLOCK

- Task receipt:
  `sha256:330d8389d0ee716347cadf2d9814f60d1ea27e42788c7b809a22d90c70e29347`.
- Descriptor-anchored snapshot/stage/source/restart attacks passed.
- Managed lifecycle set omitted the interface consumer guard and its test; status accepted a
  replaced no-op guard as current.
- Journal validation accepted an incomplete recovery set; recover returned success and removed the
  journal while omitted candidate bytes remained installed.
- Lock release did not revalidate ownership and removed a replacement owner's live lock.
- Standard gates: `19` focused and `82` full tests plus validator, Ruff, strict mypy, compilation,
  shell syntax, and diff check `PASS`.
- Evaluator verdict: `BLOCK`; independent reviewer was not dispatched.
- No review packet, ledger mutation, Git, real upgrade, release, or downstream synchronization.

## Lifecycle architecture-reset fresh review — BLOCK

- Planned receipt:
  `sha256:b68e016df7dc0fe2458351c894e52a8517aa5cb33d2429ed43e099cde64b0e01`.
- Complete closeout receipt:
  `sha256:4f104e6a2733286dacdbdeebd5acf55b66c88ef87c57ac3cc34937e676598fb5`.
- Snapshot ancestor swap redirected backup copy to an external directory before validation.
- Staged-source ancestor swap moved external content into the target and consumed the source.
- Target-side swapped-ancestor defenses passed.
- BLOCK result:
  `sha256:01b44a7754e6f2ed6326db2fbb3f9a9d16a397594c44a20d1cde8bf3c6984fbe`.
- Canonical ledger digest:
  `sha256:bed1b895c85fc52a795995afbbc0d8c3dd7a1572a41b44e8868cc552730b1105`.
- Lifecycle BLOCK count: `3`; architecture reset remains required.
- No further repair, review, Git, release, or downstream synchronization is authorized.

## Transactional-upgrade fresh review — SECOND BLOCK

- Expanded lifecycle review: `BLOCK`.
- Rollback uses `os.replace`, so each successful restore consumes its backup.
- A later restore failure left only `11/12` durable recovery assets in reviewer scratch state.
- Closeout receipt:
  `sha256:d0c82b3fe73ea65f10ae9cfc8ca164d8d971afdf06e05b3586634483024dc348`.
- BLOCK result:
  `sha256:4f487109dc6114eee0caf7054fe692fa7dc7cd3092b67830981d60f2ae8c331b`.
- Updated ledger identity:
  `sha256:8bebd2fa4949bbd79890375f7e08c8154cae7c2f6eeeb2a8a16f5ee9491bd8ff`.
- Lifecycle BLOCK count: `2`.
- Architecture reset required: `true`.
- No third local patch or fresh release decision is permitted.

## Transactional-upgrade evaluator — BLOCK

- Registered lifecycle BLOCK count remains `1`. Architecture reset is not required.
- Transactional upgrade maker: complete.
- Focused lifecycle suite: `13 passed`.
- Full public package: `76 passed`; bindings and links `PASS`.
- Strict mypy on the production manager: `PASS`.
- Ruff: `BLOCK` with four `B023` and six `SIM117` violations in the new lifecycle tests.
- Fresh review: not dispatched.
- No ledger BLOCK was recorded from this evaluator result.

## Agent+ 0.2.0 registered repair review — BLOCK

- Boundary: `AGENT_PLUS_0_2_0_RELEASE`.
- Review-closeout enforcement: declared matrix `PASS`.
- Discovery handoff durability: declared matrix and static behavioral contract `PASS`.
- Portable lifecycle integrity: declared matrix passed, but an injected mid-upgrade copy failure
  left a mixed managed-file set with the old manifest.
- Retry then refused because the installation appeared locally drifted.
- Copy failures can escape the typed manager error boundary.
- The lifecycle BLOCK was recorded after complete review closeout.
- Lifecycle BLOCK count: `1`. Architecture reset is not required.
- BLOCK result:
  `sha256:701ceec5a1945e10187cf8039ff524ad0905dc7326017bf4f4afa5b99f2d9523`.
- Updated ledger:
  `sha256:23b41c456636e2ab107bd7b099004acdc433b557c6b44e9ee5e3f71ff307f8a0`.
- Primary dossier:
  `.planning/essential-tasks/2026-08-21-agent-plus-0-2-0-release-promotion-dossier.md`.

## Surplus-capacity flock audit — BLOCK

- Direct `--record-block` can mutate a ledger without completed-review evidence.
- Closeout-mode validation produces the same receipt as generic validation.
- The current Agent+ integrated review boundary is not registered in the canonical ledger.
- A tampered install manifest can remove managed-file coverage or change source identity and still
  pass status and upgrade.
- The lifecycle omits `scripts/interface_consumer_guard.py` from managed and doctor coverage.
- Initialization can leave an unrecoverable partial control set, and brain-note YAML is not safely
  encoded.
- Discovery does not durably distinguish awaiting owner confirmation from confirmed
  `READY_FOR_CONTRACT`.
- Discovery does not persist bounded task, stop condition, and acceptance check for evidence or
  prototype exits.
- The authorized ten-chain routing experiment lacks chain-level measurement fields. Aggregate usage
  is not a valid experiment result.
- Canonical/bootstrap workflow wording differences are intentional semantic compression and are
  `NO_ACTION`.
- Primary record:
  `.planning/essential-tasks/2026-08-21-agent-plus-surplus-flock-audit-record.md`.

## Bounded recovery and unattended execution defaults

- A reviewed closed-interface recovery remains closed for its failure class.
- Reopening removed choices requires a new necessity card, named consumer, deterministic checks,
  and required review.
- An unattended run uses one authorized launch and process-written durable evidence.
- The AI controller performs zero polls while the process runs and one result check after return.
- An error or missing terminal record consumes the authorization and returns `BLOCK`.
- Retry requires bounded repair, required fresh review, and new explicit authorization.
- Canonical and bootstrap policy, protocol, and task-card fields are covered by synthetic tests.
- Focused defaults tests: 3 passed. Full public package: 63 passed; bindings and links PASS.

## Interface-consumer closure

- Schema: `agent-plus-interface-consumer-closure/v1`.
- Guard: `scripts/interface_consumer_guard.py`.
- Public example: `.agent-plus/interface-consumer-closure-example.json`.
- The guard requires the definition and every discovered repository consumer, plus a PASS for any
  required real-shape smoke.
- Unknown fields, duplicate keys, unsafe paths, missing or extra consumers, invalid ordering, and
  weak smoke status fail closed.
- Protocol/template and bootstrap copies are updated and public validation binds them.

## Receipts

- Focused tests: 7 passed.
- Agent+ unit suite: 56 passed.
- Public-package validator, canonical/bootstrap bindings, and internal links: PASS.
- Ruff and strict mypy: clean on the guard and tests.
- Guard SHA-256: `e2f30112737a25378ddbf0ff9d5fa52db49b4c4cef495063310ae38ef20271da`.

## Exact next action

Owner decides whether to hold the unreleased `0.2.0` worktree or authorize a new terminal-state
verification architecture. Do not locally repair attempt-07. Do not release or synchronize SECOND
LOOK without a later accepted implementation, fresh independent `PASS`, and explicit owner release
authorization.

## Dirty-worktree warning

Preserve all existing changes. Do not reset, clean, broad-stage, commit, push, tag, release, or
announce.

## Restart

```bash
cd agent-plus && scripts/ai-context.sh
```
