# Interface-consumer closure integrated review packet

## Review boundary

Review the generic Agent+ control and the SECOND LOOK proving-project adapter as one engineering
boundary. Do not repair, release, commit, push, tag, run certified data, or authorize a rehearsal.

## Required conclusions

1. Repository discovery cannot omit an unchanged runtime wrapper, diagnostic, launcher,
   serializer, test seam, or direct production consumer containing a declared search token.
2. Packet fields cannot widen the search through unsafe paths or hide consumers through ordering,
   duplicate keys, unknown fields, or an unapproved disposition.
3. A required real-shape smoke must be a recorded PASS before the receipt validates.
4. Canonical and bootstrap protocol, template, guard, and example remain bound by public validation.
5. SECOND LOOK's exact runtime checkpoint key is `(season: int, role: str, game_pk: int)`, and the
   measurement wrapper accepts that shape without enum assumptions.
6. SECOND LOOK checkpoint code identity includes the runner and stream-composition modules that
   decide ordering and output bytes.
7. The declared SECOND LOOK consumer receipt exactly matches deterministic discovery.

## Falsifying attacks

- Add an undeclared file under a search root that uses a declared token.
- Remove a declared consumer or add a non-discovered consumer.
- Substitute an unsafe path, duplicate JSON key, unknown nested field, unsorted path, or failed
  smoke status.
- Pass the exact runtime key `(2025, "train", 778166)` through the measured compose wrapper and
  verify the durable receipt uses `2025:train:778166`.
- Pass old or invalid shapes, including an enum assumption, wrong length, boolean season, empty
  role, or boolean game identifier, and require typed rejection.
- Remove `wave_b_runner.py` or `stream_composition.py` from checkpoint source identity and require a
  deterministic test failure.

## Maker evidence to reproduce proportionately

- Agent+: 7 focused tests, 56 full unit tests, public-package validator, Ruff, strict mypy.
- SECOND LOOK: 11 harness tests; 30 checkpoint/stream tests; Ruff; strict mypy on the three changed
  production/harness files; policy verifier; generic consumer guard.
- Confirm protected `run-1` and `run-2` remain absent and attempt-06 evidence is unchanged.

## Promotion rule

PASS accepts only this engineering guard and adapter. It does not accept Agent+ `0.2.0`, close
F-16, authorize attempt-07, authorize Wave B, authorize modeling, or authorize Git operations.

On the first decisive defect, return one integrated BLOCK naming the smallest responsible
interface. Do not split local defects into separate review cycles.
