# Agent+ Hot Memory

Updated: 2026-08-31

## Current verified state

- Canonical Agent+ `v0.3.0` is released at commit
  `ea147c0e2b2dac481238f96c37b56a31fe65033b`.
- Remote `main`, the peeled annotated tag, and the public GitHub release are verified.
- The accepted outcome-audit and legacy-adoption improvements are included in that exact release.
- The unreleased canonical worktree now includes the lean R default and one immediate correction
  for an obvious evaluator-found syntax, command-spelling, or test-harness false positive. The
  existing public package check passes all 128 tests.
- Continuous repair is now the default for routine reversible work: fix small obvious mechanical
  defects, run the smallest adequate check, and continue. Escalate only for ambiguity, adjacent
  failures at one boundary, or scientific, authority, promotion, release, or reviewer boundaries.
- The unreleased R migration guidance now requires complete-behavior parity, representative-volume
  performance checks, vectorized large-table work, cost-separated cached targets, and preservation
  of failed attempts through the existing outcome audit. Focused defaults tests and all 130 public
  tests pass; canonical/bootstrap protocol bytes match.
- Complexity is now an unreleased diagnostic rule, not a universal score gate: simplify real
  decision flow, preserve behavior with focused tests, add no score-only dependency, and do not
  scatter branches across trivial helpers. Focused defaults tests pass `6/6`; all `130` public tests
  and canonical/bootstrap parity pass.
- The unreleased active-chain and cost-isolation bundle adds one closed generic chain capsule and
  guard, live-authority references, exact target-graph isolation, deterministic `LOOP_DETECTED`,
  and `OWNER_DECISION_REQUIRED` escalation. Focused checks pass `15/15`, `54/54`, and `7/7`; all
  `147` public tests, canonical/bootstrap bindings, privacy, links, and public validation pass.
- A completion audit closed the remaining external-capsule input by accepting only the active
  capsule and sanitized example locations. Preserve
  `.planning/essential-tasks/2026-08-30-agent-plus-active-chain-completion-audit.md`.
- Fresh integrated review 001 returned `BLOCK` after all 20 attacks and 16 coverage areas. Malformed
  array or object values in enum fields can raise uncaught `TypeError` instead of typed
  `ChainError`. The completed review receipt is
  `sha256:aac523d9b1704566df547108c42802e0fcfe3189a400cac2a3c5811904496f7b7`.
- The ledger now records `block_count=1`. Post-record closeout passes with receipt
  `sha256:92ee4f60d8977d199f6207170d6f93407ec6bff550aef5e08cbe337721bf17fc`.
  The owner then authorized one new bounded repair chain.
- The typed enum repair adds string checks before the four existing membership tests. Array and
  object probes pass `8/8`; the CLI traceback probe passes; focused tests pass `17/17`; all `149`
  public tests and validation pass. Review 002 dispatch passes with receipt
  `sha256:442fe6a6499e807f854035b1c5867cc929bda3bcf1e7836cd28879b3470bfca2`.
- Fresh integrated review 002 returned `PASS` with all `21/21` attacks, `16/16` coverage areas, and
  `27/27` independent probes. It found no Critical, Important, or Minor issue. The accepted closeout
  receipt is `sha256:3ceb5acd7637c4568663a2c93216aa8b065d0ace32a723485fda1ea51be9ecfc`.
  No release, SECOND LOOK change, or consumer synchronization occurred.
- The owner authorized the active-chain lifecycle-delivery closure. Manifest v2 now binds one exact
  managed-set identity and accepts only the exact released v1 set for `0.2.0` or `0.3.0` history.
  Upgrades are additive only, preserve executable modes, reject collisions, and recover only the
  candidate additions that the transaction committed.
- Clean initialization now delivers and manages the guard and sanitized example. Canonical and
  copied doctors verify exact sets, digests, safe paths, and command modes. Lifecycle tests pass
  `42/42`; legacy-adoption tests pass `12/12`; all `163` public tests and validation pass.
- The lifecycle review packet was admitted with receipt
  `sha256:52807ea70a2ba916c4defcf1a3053e429a2dbb72ccff39c07ac03ac757e0cca5`.
  One fresh Luna xhigh reviewer returned `PASS`: all `27/27` attacks and `18/18` named coverage
  items passed, with no Critical, Important, or Minor finding. The accepted complete-review receipt
  is `sha256:c77ba7104f89e9ebbaa693c342a3dd940b9e8576b2457032252c7a1038136d10`.
- A read-only acceptance-gap audit isolated the remaining unreleased review gap to Lean R,
  small-correction authority, uncovered R-migration semantics, complexity guidance, and versioning.
  The policy review contains `22` attacks and `10` coverage items. Task admission passes
  with receipt `sha256:3a9458c5004852adfd21fe3978f0b02cb3676b7f449fbb061835ac6a3477c2c5`;
  workflow defaults pass `7/7`, and the full public suite and validator pass `163/163`.
- One fresh Luna xhigh reviewer returned `PASS`: all `22/22` attacks, `10/10` coverage items, and
  `35/35` independent semantic probes passed with no Critical, Important, or Minor finding. The
  accepted closeout receipt is
  `sha256:c348f93bd675dd423933597508d4ffe5446758531fc45fff331e40231acc7ff8`.
- Against the current ledger, the acceptance matrix still reproduces the active-chain, lifecycle,
  and policy closeouts as `PASS` with receipts
  `sha256:7a2471dfcf6c349b21180ff80108b5bcf200ddf5029d22d1e304fff35d6e2bea`,
  `sha256:ff348b51a8076e500bbbce5fa9f744f87bf7bee052b27a77eb50a8fcb413e460`,
  and `sha256:072b74fd7d1a0561ee7086998e9e949da10934c0f547f134d90d76bf187dccc8`.
  The static-typing review returned `BLOCK` and does not make the complete bundle accepted.
- A read-only `0.4.0` release-scope audit classified every dirty path. The initial 67-path candidate
  manifest had SHA-256
  `15f6e5b7efaedfeb93fa15244dbd2bdb0e2cbbb6eab46c74d43c7c1e182a49be`.
- Two adjacent temporary-index harness failures triggered `LOOP_DETECTED`; one bounded sweep closed
  the colon-sensitive object-store path through a temporary symlink.
- Simulated `VERSION=0.4.0` exposed one eligible test-fixture false positive. The single correction
  pins synthetic v1 history to released `0.3.0`; production code and authority are unchanged. The
  corrected test hash is `91043db3fc817a0fc03fd269a80572d5f3792d02e40f13e6052392c19793217f`.
- The corrected disposable candidate passes `163/163`, public validation, privacy, links, bindings,
  executable modes, exact scope, and diff checks. The real index stayed byte-identical. Local
  `HEAD` and remote `main` remain the `0.3.0` release commit; `v0.4.0` tag and release are absent.
- A later static gate returned `BLOCK`: Ruff passes, but strict mypy reports exactly two manifest
  identity narrowing errors in `scripts/agent_plus_manager.py`. Because the preflight had already
  consumed its one test-fixture correction, no same-chain repair followed. The registered bounded
  repair `AGENT_PLUS_RELEASE_READINESS/manifest-strict-mypy-narrowing` is now active.
- The bounded repair narrows the v2 managed-set value only after exact runtime validation and gives
  `_manifest_set_id` a typed malformed-input rejection. Strict mypy, Ruff, compilation, `43/43`
  lifecycle tests, `12/12` legacy tests, `164/164` complete tests, and public validation pass. One
  fresh `8`-attack, `7`-coverage review remains required.
- The repaired exact candidate now contains `72` paths. Its manifest SHA-256 is
  `1293401367fa7a26d62ec058184b3a4720d92b64bf2587be5bf89e4bc8836b07`. A disposable simulated
  `VERSION=0.4.0` passes `164/164`, public validation, strict mypy, Ruff, exact scope equality, and
  unchanged-real-index checks. Planned review admission receipt:
  `sha256:c40a3a6055e1c48955360be3731605cb9cfda044341b9d656b799bf8b83b4ccd`.
- Fresh review 001 assessed all `8/8` attacks and `7/7` coverage items. The typed seam, strict mypy,
  Ruff, focused tests, and live `164/164` public validator passed. The required disposable
  simulated-version validator did not start because `tar` returned `Special header too large`.
  The reviewer returned `BLOCK` without repair. Closeout receipt:
  `sha256:ab87a2b1d035f1337a2686bffc9b3bf297386d8e7407912198327e4463a287e7`.
- The controller recorded the review `BLOCK` with result
  `sha256:d704c9223ebc44444dac3a39ce49dd0ab5cc33a349ad5a37846744d7a359e2fa`.
  The ledger now has `block_count=2` and requires architecture reset. The active chain closes with
  terminal `BLOCK`. The exact current candidate contains `73` paths with manifest SHA-256
  `8e96619ecc5df7feaf6ab77769475b9b7d7e3e8199e58ced8eb68f43d18ea1a0`.
- Owner-authorized release-preflight architecture reset attempt 002 added one zero-option verifier,
  fixed config, tests, docs, and public-validator integration. The exact candidate grew to 79 paths
  with manifest SHA-256
  `3f533be8ad2296e9d711cecfff3e42b8258df405751887f706a8f43fa024e7ef`. Focused tests passed `7/7`,
  strict mypy and Ruff passed, and the complete public validator passed `171/171`.
- The reset's one permitted real launch returned
  `RELEASE_CANDIDATE_BLOCK E_LIVE_OBJECT_MUTATION`. No receipt was written. `VERSION`, `HEAD`, and
  the real index hash remain unchanged. The timestamp scan found 75 loose object files: 65 blobs
  and 10 trees, with 66 candidate paths mapping to those blob identities. It did not prove new
  objects or a concurrent writer. A synthetic reproduction established that Git alternate-object
  freshening can change existing timestamps without changing names or bytes. Attempt 002 remains
  BLOCK and may not be repaired or rerun.
- Owner-authorized detached successor attempt 003 passed real deterministic preflight and fresh
  independent Sol/high review: 26/26 attacks, 17/17 coverage areas, and 50/50 independent probes.
  The complete candidate suite passed 196 tests, including 32 focused verifier tests. Strict mypy,
  Ruff, bindings, privacy, links, exact scope, candidate integrity, and source preservation passed.
  The evaluator and reviewer matched the same 81-path candidate tree. Current typing/helper and
  lifecycle-fixture deltas are included in this acceptance, not inferred from historical packets.
  The complete anti-loop closeout receipt is
  `sha256:bdb2fdeff215ba00c450f6acf3a0f8b472c8392d4de8f88d31a26baf3990d8a4`.
  Final administrative bytes require the one authorized success-only final refresh. Read the
  current receipt at `outputs/release-candidate/receipt.json` before any publication decision.
  Preserved evaluator/reviewer receipts and the full report are in `outputs/release-candidate/`.
  The owner teaching packet is `outputs/optimization-review-2026-08-30/MASTER_PACKET.md`.
- On 2026-08-31 the owner authorized one exact public `v0.4.0` release attempt: refresh the
  preflight once, stage only the 81-path manifest with simulated `VERSION=0.4.0`, commit on `main`,
  create annotated tag `v0.4.0`, push both atomically, publish one public GitHub release, and verify
  all remote identities. Preserve and locally exclude the active article discovery brief. Exclude
  outputs, locks, local routing/registry state, editor metadata, consumers, data, and science.
  Stop at the first failed or uncertain release operation. Do not retry or synchronize consumers.
- Publication attempt 001 stopped at its single refreshed preflight with
  `E_SOURCE_MUTATION`. Receipt:
  `sha256:5cfbd1a2eb1d3c9a713b7d622a1afcd3bed0dc5616e5d4de4e5d47f6e769a1f3`.
  The protected source changed during verification. The receipt does not identify the actor or
  path. No staging, commit, tag, push, GitHub release, consumer change, or science followed. Local
  `HEAD`, the real index, remote `main`, and released `v0.3.0` remain at
  `ea147c0e2b2dac481238f96c37b56a31fe65033b`; `v0.4.0` remains absent.
- The owner authorized publication attempt 002 to continue tonight. It must freeze the same 81
  source paths, copy only those paths into a clean isolated checkout at the verified remote base,
  confirm source hashes/modes before and after transfer, and run one refreshed closed preflight in
  isolation. Only a PASS permits exact staging, `VERSION=0.4.0`, one commit, annotated tag, atomic
  push, GitHub release, and remote verification from that checkout. Stop at the first error. The
  active source checkout, article discovery brief, consumers, data, and science remain untouched.
- Versioning rule of thumb: use `0.3.x` for small compatible fixes and refinements. Reserve a new
  `0.x.0` minor release for a genuinely new public capability or authority boundary.
- Preserve every dirty-worktree change. Do not reset, clean, broadly stage, commit, push, tag,
  release, or synchronize without exact owner authority.

## Outcome-audit capability — accepted

- Attempt 04 fresh integrated review: `PASS`; all `25/25` attacks and `15/15` named coverage items
  passed.
- Focused suite: `18/18`. Complete suite and public validator: `126/126`.
- Accepted implementation hashes:
  - `skills/outcome-audit/outcome_audit.py`:
    `e1b94b1b8c6ad138ac7e8d7d2b0ac6d13d4175e52fcc771cac90406a448fc663`
  - `tests/test_outcome_audit.py`:
    `b92e96cb35d2d735979dadf5aa55a753abf9737f52562e387095afe6c5255cbf`
  - `skills/outcome-audit/SKILL.md`:
    `d694b45ca1596336cdd25f93c9f701f808d32a22eb28b9c07aaea6b1defa6c31`
- Integrated review:
  `.planning/essential-tasks/2026-08-22-agent-plus-outcome-audit-integrated-review-004.md`.
- Accepted closeout receipt:
  `sha256:ef242f6a2f605a1b112a67d1d1f128d3451480ef463c0c38c4c53cf3901c0a13`.

## Legacy-adoption capability — accepted

- Attempt 003 fresh integrated review: `PASS`; all `13/13` attacks and `15/15` named coverage items
  passed.
- FIFO hooks reject within two seconds across adopt, status, both doctors, and both contexts.
- First, middle, and last managed-manifest omissions reject across every named consumer before
  context output.
- Focused suite: `11/11`. Lifecycle suite: `29/29`. Complete suite: `128/128`.
- Integrated review:
  `.planning/essential-tasks/2026-08-22-agent-plus-legacy-adoption-integrated-review-003.md`.
- Accepted closeout receipt:
  `sha256:09058cb34a4cc6b4d36efa4d740825cbe08ff8fb364bb404e73b079e3c8640e6`.
- No consumer synchronization, SECOND LOOK change, or scientific work followed the release.

## Public example

- Phase 1 public example coverage remains `BLOCK-COVERAGE-EXAMPLE`.
- Modeling remains forbidden.

## Exact next action

Execute owner-authorized publication attempt 002 from one isolated release checkout as recorded in
the release-readiness dossier. Attempt 001 remains spent. Stop at the first error and do not retry.
Do not synchronize a consumer.

Restart command from repository root:

```sh
./scripts/ai-context.sh
```
