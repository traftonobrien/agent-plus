---
task: "Bundle the FIFO-safe hook and exact copied-target manifest repair"
owner_role: "maker"
tier: "2"
time_budget: "90 minutes maker; 45 minutes reviewer"
attempt_limit: 1
orchestration_mode: "deep"
exact_model: "Sol control plane; Luna xhigh maker; deterministic evaluator; different fresh Luna xhigh reviewer"
reasoning: "implementation"
authorization_scope: "Owner authorized attempt-03 bundled architecture repair for FIFO-safe hook validation and one exact copied-target manifest verifier; no release or consumer synchronization"
ownership_map: "Sol registers and evaluates; one Luna xhigh writer owns implementation; one different fresh Luna xhigh verifier owns integrated review; owner retains release authority"
chain_stages: "necessity -> dispatch guard -> maker -> deterministic evaluator -> fresh review -> Sol synthesis -> owner release decision"
concurrency: "one writer; sequential evaluator and reviewer; no recursive spawning"
tool_budget: "40 maker calls or 90 minutes; 20 reviewer calls or 45 minutes"
context_budget: "25K ordinary ceiling; 8K-15K implementation target per agent"
reserve_use: "25% premium reserve preserved"
checkpoint_interval: "one bounded wait at maker and reviewer boundaries; no polling"
overnight: "no"
unattended_execution: "no"
unattended_command: "not-applicable"
durable_evidence: "this card, deterministic receipt, fresh review record, and hot-memory closeout"
return_check: "not-applicable"
polling_policy: "not-applicable"
evaluator_commands: "focused lifecycle/context/doctor tests; full public tests; public validator; Ruff; strict mypy; compilation; shell syntax; managed bindings and public-safety checks"
fresh_verifier_contract: "independent exact changed bytes and evaluator receipts; complete registered attack matrix; no maker transcript; no repair"
promotion_dossier: ".planning/essential-tasks/2026-08-22-agent-plus-legacy-adoption-hook-and-doctor.md"
anti_loop_packet: ".agent-plus/agent-plus-legacy-adoption-architecture-task-003.json"
review_closeout: ".agent-plus/agent-plus-legacy-adoption-architecture-review-003.json"
---

# Agent+ legacy adoption hook and doctor seam

## Attempt 003 bundled interface repair

- **Decision:** Replace blocking hook opens and duplicate copied-target manifest parsers with one nonblocking hook validator and one already-managed doctor verifier reused by context.
- **Existing findings:** Review 002 proved that a FIFO hook can block before `fstat` and that copied doctor/context accept subset managed-file manifests.
- **Guard lane:** The ledger has one recorded BLOCK, so this runs as the permitted bundled `local_repair`; an architecture-reset packet is not authorized.
- **Minimum architecture:** The manager uses nonblocking open only for the fixed startup hook. The already-managed doctor exposes one internal copied-target check with exact 13-file set validation, nonblocking non-following hook open, `fstat`, and descriptor-bound execution. Canonical and bootstrap context source that doctor check instead of carrying duplicate legacy manifest/hook parsers.
- **Removed choices:** `blocking-startup-hook-open`, `duplicate-legacy-manifest-parser`, and `subset-managed-set-acceptance`.
- **No migration:** Do not add a managed file, schema version, initializer mode, hook path, configuration, or caller option.
- **Allowed write surface:** `scripts/agent_plus_manager.py`, `scripts/agent-plus-doctor.sh`, `scripts/ai-context.sh`, `bootstrap/base/scripts/ai-context.sh`, and `tests/test_agent_plus_legacy_adoption.py`, plus controller-owned dossier and attempt packets.
- **Frozen adjacent boundary:** Outcome-audit implementation, tests, skill, accepted review, and `scripts/validate-public-package.sh` must remain byte-identical to their attempt-04 accepted hashes.
- **Acceptance additions:** FIFO without a writer rejects within two seconds for adopt, status, canonical doctor, copied doctor, and context. Removing first, middle, or last managed entry rejects in canonical status/doctor, copied doctor, and context with no context header. Existing exact-byte, hook-exit, symlink, directory, descriptor-swap, initialized behavior, state routing, upgrade, and public validation remain green.
- **Stop:** Maker, evaluator, or reviewer `BLOCK` ends attempt 003. No same-chain repair.
- **Owner authorization:** The user said, “Proceed,” on 2026-08-22.

## Necessity

- **Decision:** Can Agent+ safely adopt an existing consumer without weakening its automatic project checks or requiring initializer-only state?
- **Uncertainty:** The released manager can record a manifest over divergent target bytes, the managed context script has no fixed project hook, and doctor assumes initializer output.
- **Existing evidence:** A sanitized consumer audit returned `BLOCK` on these generic interfaces. Agent+ v0.2.0 remains valid and released but cannot safely adopt that legacy shape.
- **Minimum action:** Add one fixed validated project-owned startup-check seam, one exact-byte legacy adoption command, and one doctor route for that declared adoption mode.
- **Unlock:** A later exact Agent+ release can receive a separately authorized SECOND LOOK adoption attempt.

## Boundary

- **Allowed artifacts:** `scripts/agent_plus_manager.py`, `scripts/agent-plus`, `scripts/agent-plus-doctor.sh`, `scripts/agent-plus-init.sh`, managed bootstrap context files, lifecycle/public tests, public documentation, validator bindings, this task packet, evaluator receipt, review packet/record, and local hot memory after PASS.
- **Allowed actions:** Implement public-safe generic lifecycle, context, doctor, tests, and documentation changes with synthetic fixtures only.
- **Forbidden actions:** SECOND LOOK edits; consumer adoption; private paths/state; Git staging, commit, tag, push, or release; scientific work; data access; automatic retry; reviewer repair.
- **Review mode:** Engineering sweep.
- **Failure-class coverage:** `legacy-adoption-hook-and-doctor-seam` and adjacent manifest, context, doctor, initialization, upgrade, and preservation behavior.
- **Simplification trigger:** A second review BLOCK at this interface requires architecture simplification. This first chain permits one bundled implementation only.
- **User-visible outcome:** Agent+ can safely admit an existing project with exact managed bytes and preserve its fail-closed project startup check.
- **Stop condition:** Maker, evaluator, or fresh reviewer returns one explicit `PASS` or `BLOCK`.
- **Stop behavior:** Complete the registered engineering matrix where safe; no same-chain repair after review BLOCK.
- **Attack matrix:** Exact managed bytes before manifest; divergent-byte rejection; project-owned preservation; fixed hook path; hook failure before context output; hook symlink/non-regular rejection; absent required legacy hook and optional initialized hook behavior; legacy doctor without initializer examples; initialized doctor unchanged; status exactness; upgrade seam preservation; partial-adoption fail-closed behavior; public validator.
- **Named coverage:** Lifecycle CLI; manager record/adopt interface; manifest; adoption declaration; context script; project hook; doctor; initializer; status; upgrade/recovery; sync workflow; bootstrap; docs; tests; public validator.
- **Promotion dossier:** This file remains the stable implementation/evaluation/review record.
- **Dispatch guard:** New ledger failure class plus planned packet PASS required before maker dispatch.
- **Review closeout guard:** Complete task-to-review matrices and closeout guard PASS required before acceptance.
- **Changed interfaces:** One explicit legacy-adoption command; one fixed project-owned context-check contract; doctor recognition of the declared legacy-adoption mode. Exact symbols and persisted shapes must be documented by the maker.
- **Consumer discovery:** `scripts/`, `bootstrap/`, `skills/agent-plus-sync/`, `docs/`, `README.md`, and `tests/` must be searched for lifecycle, context, doctor, manifest, and adoption consumers.
- **Consumer closure:** Every discovered CLI, initializer, status, doctor, upgrade/recovery, sync-skill, bootstrap, test, validator, and documentation consumer must be migrated or explicitly unchanged with evidence.
- **Runtime wrappers:** Shell CLI and doctor call the Python lifecycle manager. No dynamic patch, arbitrary callback path, reflection, or `Any`-typed adapter is permitted.
- **Real-shape smoke:** Use disposable synthetic initialized and legacy targets, including paths with spaces, exact-byte adoption, project-check PASS/BLOCK, doctor, status, upgrade preservation, and malformed/symlink attacks.
- **Repeat trigger:** Changed implementation after evaluator BLOCK, changed architecture after reviewer BLOCK, or explicit owner release authorization after reviewed PASS.
- **Acceptance:** Full registered matrix, focused and full tests, validator, Ruff, strict mypy, compilation, shell syntax, public safety, and fresh independent integrated review all PASS.

## Maker implementation contract

- Command: `scripts/agent-plus adopt --target DIRECTORY --profile research|product|scouting`.
  The command acquires the normal Agent+ lock, verifies every manifest-managed target file against
  the current canonical source bytes, then publishes the declaration and manifest. It refuses a
  divergent, missing, symlinked, or non-regular managed surface and never copies project-owned files.
- Adoption declaration: `.agent-plus/legacy-adoption.json`, exact JSON fields
  `schema_version`, `mode`, `profile`, and `startup_check_path`; values are respectively
  `agent-plus-legacy-adoption/v1`, `legacy-adopted`, one allowed profile, and the closed literal
  `.agent-plus/project-startup-check.sh`. The declaration is written before the manifest so a
  partial adoption remains visible and fails closed.
- Manifest: existing `.agent-plus/install-manifest.json` schema
  `agent-plus-install/v1`; adoption writes the same exact canonical managed-file set and hashes.
  An existing manifest or adoption declaration is not overwritten by `adopt`.
- Startup seam: `.agent-plus/project-startup-check.sh` is project-owned and required for a declared
  legacy adoption. `adopt`, `status`, `doctor`, and `scripts/ai-context.sh` validate that it is an
  executable regular non-symlink and context runs it before any managed context output. Ordinary
  initialized projects may omit it. The caller cannot select another path.
- Legacy doctor: the Python lifecycle manager and standalone copied doctor validate the declaration,
  manifest, managed regular files, and required startup seam without requiring initializer-only
  project examples. Normal initialized-project doctor checks remain unchanged.
- Partial adoption: missing or malformed declaration/manifest, mismatched profiles, managed drift,
  or unsafe startup seam returns `AGENT_PLUS_ERROR`/non-zero and produces no success status.

## Decision branches

- **If PASS:** Record reviewed upstream capability and stop for a separate release decision.
- **If BLOCK or FAIL:** Preserve the complete finding and stop without release or consumer synchronization.

## Maker result — PASS

The Luna xhigh maker added the exact-byte `adopt` command, fixed startup-check declaration,
legacy-doctor route, managed context integration, synthetic tests, public documentation, sync
guidance, and validator bindings. Maker-focused validation passed. This is maker evidence only.

## Deterministic evaluator — BLOCK

The evaluator completed the safe registered checks. Focused, lifecycle, full public, validator,
Ruff, strict mypy, compilation, shell syntax, binding, link, and diff checks passed.

An evaluator-owned disposable target then proved that doctor passes when the required startup check
exits `7`. It also proved that adopted context omits existing `.claude-memory.md` and
`.planning/STATE.md` state. Descriptor-bound hook execution remains unproven.

Evidence:
`.planning/essential-tasks/2026-08-22-agent-plus-legacy-adoption-evaluator-block.md`.

Status: `AGENT_PLUS_LEGACY_ADOPTION_EVALUATOR_BLOCK — REVIEW NOT DISPATCHED`.

## Owner-authorized bundled repair — attempt 002

The owner authorized one new engineering-only bundled repair after the evaluator `BLOCK`.

The repair must close all three proved defects and the adjacent open invariant in one maker
boundary:

1. Doctor executes the fixed project startup check and requires exit status `0` before `PASS`.
2. Adopted context preserves existing `.claude-memory.md` and `.planning/STATE.md` routing.
3. Hook execution is bound to the validated regular file bytes. A path replacement cannot select
   different bytes after validation.

The repair must preserve exact managed-byte admission, project-owned files, initialized-project
behavior, status, upgrade/recovery, and the partial-adoption fail-closed contract. It receives one
complete deterministic evaluator rerun and one different fresh Luna xhigh integrated review.

Dispatch packet:
`.agent-plus/agent-plus-legacy-adoption-repair-task-002.json`.

No Git operation, release, consumer adoption, SECOND LOOK change, or same-review repair is
authorized.

## Repair maker result — attempt 002

Implemented the bundled repair: canonical and copied doctor now execute the fixed hook and require
exit `0`; context runs the validated hook bytes through an inherited descriptor and includes existing
legacy hot-memory and planning-state files while safely omitting absent files; deterministic swap,
exit-7, sentinel-routing, copied-doctor, canonical-doctor, and initialized-project tests were added.
Maker-focused validation passed. This is maker evidence only; deterministic evaluator rerun and
fresh independent review remain required.

## Deterministic evaluator — attempt 002 PASS

The complete evaluator rerun passed: `9` focused legacy-adoption tests, `29` lifecycle tests,
`108` full public tests, public validation, Ruff, strict mypy, compilation, shell syntax, bindings,
links, and diff checks. The original doctor exit-status, hot-memory routing, planning-state routing,
and descriptor-bound execution findings are closed by executable synthetic tests.

Evidence:
`.planning/essential-tasks/2026-08-22-agent-plus-legacy-adoption-evaluator-pass-002.md`.

Status: `AGENT_PLUS_LEGACY_ADOPTION_EVALUATOR_PASS_002 — FRESH REVIEW REQUIRED`.

## Fresh integrated review — attempt 002 BLOCK

One different fresh Luna xhigh reviewer completed all `13/13` registered attacks and all `15/15`
named coverage items. Eleven attacks passed. Two promotion-blocking defects remain:

1. A FIFO startup hook can block before the non-regular-file rejection.
2. The copied doctor and context routes accept a subset managed-file manifest that the canonical
   manager correctly rejects.

Evidence:
`.planning/essential-tasks/2026-08-22-agent-plus-legacy-adoption-integrated-review-002.md`.

Status: `AGENT_PLUS_LEGACY_ADOPTION_REVIEW_002_BLOCK — NO RELEASE OR CONSUMER ADOPTION`.

Closeout guard: `PASS`, receipt
`sha256:b573dff2c59eb6c208c3fcfb49bb01b8a05b10f1742913ccab9b0ca84867672b`.
The ledger recorded the reviewer BLOCK as block count `1`, result
`sha256:431fd767b2791b3c3e3d62a3e1e42fadc23aef1a7507e91d64d0d3f9cab9a930`.

## Closeout

```yaml
decision_changed: no
blocker_closed: original evaluator findings closed; integrated boundary remains blocked
work_unlocked: none
user_visible_outcome: none; legacy adoption remains blocked
repeat_trigger: new owner authorization for one bundled repair of the reviewer findings
```

## Owner-authorized bundled interface repair — attempt 003

The owner authorized one bounded engineering-only repair of the two attempt-002 reviewer findings.
The accepted design keeps one copied-target verifier in `scripts/agent-plus-doctor.sh`, requires the
exact 13-file managed set in every consumer, and opens the fixed startup hook nonblocking before
regular-file validation. The manager change is limited to a private nonblocking option used by the
fixed startup-check opener. No new managed file, schema, mode, path, option, or initializer behavior
is introduced.

Dispatch packet:
`.agent-plus/agent-plus-legacy-adoption-architecture-task-003.json`.

Anti-loop guard: `PASS`, receipt
`sha256:c38dcdd7c156e5e2e6bb35fb980db6417614a4113d82f1fa065e0317211d4213`.

## Repair maker result — attempt 003 PASS

The Luna xhigh maker implemented the bounded interface repair. This is maker evidence only.

## Deterministic evaluator — attempt 003 PASS

Independent deterministic evaluation passed:

- `11/11` focused legacy-adoption tests, including FIFO rejection within two seconds for adopt,
  status, canonical doctor, copied doctor, canonical context, and bootstrap context.
- First, middle, and last managed-manifest omissions are rejected by every named consumer before
  context emits an `AGENTS.md` header.
- `29/29` lifecycle tests and `128/128` complete tests.
- Public-package validation, Python compilation, and POSIX shell syntax checks.
- Frozen outcome-audit implementation, tests, skill, accepted review, and public-validator hashes
  remain exact.

Status: `AGENT_PLUS_LEGACY_ADOPTION_EVALUATOR_PASS_003 — FRESH REVIEW REQUIRED`.

## Fresh integrated review — attempt 003 PASS

One different fresh Luna xhigh reviewer completed all `13/13` registered attacks and all `15/15`
named coverage items. The reviewer independently proved that no-writer FIFO hooks reject within two
seconds across every named route and that first, middle, and last managed-manifest omissions reject
across every named consumer before context output.

Focused tests passed `11/11`; lifecycle tests passed `29/29`; the complete suite and final public
validator passed `128/128`. Compilation, shell syntax, direct disposable-target attacks, and frozen
adjacent hashes passed.

Evidence:
`.planning/essential-tasks/2026-08-22-agent-plus-legacy-adoption-integrated-review-003.md`.

Status: `AGENT_PLUS_LEGACY_ADOPTION_REVIEW_003_ACCEPTED — NEW RELEASE DECISION REQUIRED`.

Closeout guard: `PASS`, receipt
`sha256:09058cb34a4cc6b4d36efa4d740825cbe08ff8fb364bb404e73b079e3c8640e6`.

No release, Git operation, consumer synchronization, SECOND LOOK change, or scientific work is
authorized by this acceptance.
