# Agent+ public state

The 0.4.2 patch packages control-integrity repairs on the published 0.4.1 baseline.
Read `docs/releases/0.4.2.md` and `docs/control-guarantees.md` for scope and limits.
Release configuration: `.agent-plus/release-candidate.json`.
Scientific work remains BLOCK-COVERAGE-EXAMPLE. Performance gains remain unmeasured.
Consumer upgrades require an exact official release and target-specific authorization.
Restart: `./scripts/ai-context.sh`.
