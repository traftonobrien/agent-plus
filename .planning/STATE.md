# Public Example State

Updated: 2026-08-22

## Current phase

Phase 1: Public example research gate.

## Verified state

- Contract: `EX-CONTRACT-001 v1`.
- Deterministic evidence record: `EX-EVID-001`.
- Required illustrative partitions: 12.
- Located illustrative partitions: 11.

## Current outcome

`BLOCK-COVERAGE-EXAMPLE`

An independent inventory receipt is absent. Complete coverage is unverified. Modeling is forbidden.

## Editorial pass and implementation subtraction — review accepted (2026-08-22)

- Added one optional, attributed `editorial-pass` skill with detect, edit, and draft modes.
- Pinned the inspected `no-ai-slop`, `Slopbeth`, and `anti-slop` commits and preserved their MIT notices.
- Added a deterministic protected-token checker and six synthetic tests.
- Added an attributed Ponytail-inspired implementation subtraction ladder to the canonical and
  bootstrap essential-work protocols.
- The focused suite and full 98-test public package passed.
- One fresh Luna xhigh reviewer passed all 12 attacks and 11 named coverage items. The completed
  packet passed exact ledger-bound review closeout.
- The existing Agent+ lifecycle `BLOCK` still forbids release and SECOND LOOK synchronization.

Status: `AGENT_PLUS_EDITORIAL_PASS_AND_SUBTRACTION_REVIEW_ACCEPTED — RELEASE BLOCKED`.

## Terminal recovery authentication — review accepted (2026-08-22)

- Attempt-08 requires exact installed-byte authentication before terminal recovery cleanup.
- The complete unattended evaluator passed all 12 gates.
- One fresh Luna xhigh reviewer passed all 42 lifecycle attacks and 32 named coverage items.
- The completed ledger-bound review receipt is
  `sha256:331cc2b02681c81acaba8aee90b9c84042dc823b0ca80f582700b15007cef038`.
- The accepted editorial records remained byte-identical to their accepted baselines.
- No release, Git operation, publication, real upgrade, or SECOND LOOK synchronization occurred.

Status: `AGENT_PLUS_TERMINAL_RECOVERY_AUTHENTICATION_REVIEW_ACCEPTED — OWNER RELEASE DECISION REQUIRED`.

## Agent+ sync skill — maker complete (2026-08-22)

- Added one optional `$agent-plus-sync` skill with audit, authorized sync, and verify modes.
- It requires an exact official release and uses the accepted transactional lifecycle.
- It preserves project-owned controls and stops without automatic recovery after an error.
- The skill validator, 99 public tests, and 29 lifecycle tests passed.
- No consumer project or SECOND LOOK file changed.

Status: `AGENT_PLUS_SYNC_SKILL_MAKER_COMPLETE — FRESH REVIEW REQUIRED`.

The first complete release-candidate evaluator stopped at Ruff after 10 completed gates. No repair,
retry, reviewer dispatch, or release action followed.

Status: `AGENT_PLUS_SYNC_SKILL_EVALUATOR_BLOCK — NEW AUTHORIZATION REQUIRED`.

The owner authorized the bounded Ruff correction. Release-candidate evaluator run-2 passed all 15
gates. One fresh integrated review remains required before release.

Status: `AGENT_PLUS_SYNC_SKILL_EVALUATOR_PASS — FRESH INTEGRATED REVIEW REQUIRED`.

One fresh Luna xhigh reviewer accepted the complete sync, editorial, and lifecycle boundary. Sync
passed `16/16` attacks and `15/15` named coverage items. Editorial passed `12/12` and `11/11`.
Lifecycle passed `42/42` and `32/32`. The sync closeout receipt is
`sha256:e405c507689d44a2180d483b1650dc0ab67f6faa6e91d54ab3e02036cccf1c41`.

Status: `AGENT_PLUS_SYNC_INTEGRATED_REVIEW_ACCEPTED — OWNER RELEASE DECISION REQUIRED`.

The exact 134-file public release scope passed a temporary-index diff check. Local-only files and
generated evidence are excluded. Remote `main` matches the local base, and the `0.2.0` tag does not
exist.

Status: `AGENT_PLUS_0_2_0_RELEASE_READY — EXPLICIT RELEASE AUTHORIZATION REQUIRED`.

## Exact next action

Add a synthetic independent inventory receipt only if it is clearly labeled as illustrative. Then rerun the public validator and request a new synthetic verifier record.

## System routing update — 2026-08-15

- Agent+ now supports direct Sol-managed, model-selected Luna delegation when the owner explicitly
  requests delegation, sub-agents, parallel work, or Luna.
- Luna High is the routine maker. Luna xhigh owns complex engineering, long read-only work, and
  fresh engineering review. Sol remains the control plane and premium reasoning remains reserved
  for scientific or irreversible decisions.
- Every delegated task requires bounded context, explicit ownership, a deliverable, a stop
  condition, and a budget. Parallel workers must be independent and may not self-certify.
- The same rules are present in the public Agent+ policy and the new-project bootstrap files.
- Repeat this decision only when model access, rates, delegation support, or a bounded benchmark
  changes.

## Bounded recovery and unattended execution defaults — maker complete (2026-08-21)

- A reviewed closed-interface recovery remains the default for its failure class. Removed caller
  choices cannot return without a new necessity card, named consumer, deterministic checks, and
  required review.
- An unattended run uses one authorized launch, process-written durable evidence, zero AI polling,
  and one result check after return.
- An error or missing terminal record consumes the authorization and returns `BLOCK`. Fallback,
  relaunch, or retry requires bounded repair, required fresh review, and new explicit authorization.
- The public and bootstrap controls contain the same generic contract. Synthetic defaults tests
  passed 3/3. The full public package passed 63 tests, canonical/bootstrap bindings, and link checks.
- No project path, identity, count, scientific detail, receipt, or runtime state moved upstream.

Status: `AGENT_PLUS_BOUNDED_RECOVERY_UNATTENDED_DEFAULTS_MAKER_COMPLETE — FRESH REVIEW REQUIRED`.

## Engineering closure update — 2026-08-15

- Engineering diagnostics now block promotion at the first defect but continue bounded read-only
  attacks to collect the complete reachable failure class.
- One consolidated finding set routes to one bundled maker repair, one deterministic receipt, and
  one fresh integrated review.
- A second BLOCK at the same interface forbids another local patch and forces interface
  simplification or replacement.
- Scientific, live, destructive, and irreversible execution remains fail-closed at the first
  invalid condition.
- Success is the user-visible or scientific milestone unlocked; tests and review records are
  evidence, not the outcome.

## Anti-Loop Guard v1 — maker complete (2026-08-15)

- `scripts/anti_loop_guard.py` validates engineering-sweep and scientific/live first-error packets
  against `.agent-plus/engineering-boundaries.json` with typed failures and a stable PASS receipt.
- The guard is wired into public-package validation, bootstrap copies, generated-project doctor,
  and the active SECOND LOOK policy verifier.
- Focused deterministic tests pass (16), including BLOCK tracking, third-local-repair rejection,
  architecture-reset acceptance, malformed/unknown ledger state, packet/ledger mismatch,
  incomplete engineering coverage, first-error preservation, and bootstrap behavior.
- Maker receipt: `.planning/essential-tasks/2026-08-15-engineering-closure-loop-repair-maker-receipt.md`.
- Fresh integrated review packet:
  `.planning/essential-tasks/2026-08-15-engineering-closure-loop-integrated-review-packet.md`.

Status: `AGENT_PLUS_ANTI_LOOP_GUARD_V1_MAKER_COMPLETE — FRESH INTEGRATED REVIEW REQUIRED`.

## Anti-Loop Guard v1 — blocked twice (2026-08-16)

Two independent fresh reviews returned `AGENT_PLUS_ANTI_LOOP_GUARD_V1_REVIEW_BLOCKED`. v1 decided
authorization from natural-language wording, so disguised process-only outcomes passed, nested
caller-controlled values could relabel a local patch as an architecture reset, and direct mapping
inputs with non-string keys raised a raw `TypeError`. That is the second BLOCK at the same
interface, so a third local patch is forbidden.

## Anti-Loop Guard v2 — maker complete (2026-08-16)

- v2 replaces the decision interface with one closed, strictly structured schema
  (`anti-loop-guard/v2`). Every authorization field is a fixed enum or an exact ledger-approved
  identifier. The packet carries no free text, and the v1 keyword and relabel logic is removed.
- The ledger owns the BLOCK count, reset requirement, approved reset namespace, authorized attempt
  identifier, permitted removed caller choices, permitted outcome identifiers and categories, and
  both required matrices.
- The input boundary is closed: string mapping keys at every depth, no duplicate keys, no unknown
  fields, bounded nesting, exact non-negative integer counters, and typed errors for malformed
  JSON, NaN, infinity, and unsupported schema versions.
- Preserved from v1: ledger-bound matrices, lock-serialized concurrent BLOCK recording, atomic
  crash-recoverable writes, the canonical/bootstrap hash binding, non-arbitrary reset namespaces,
  and deterministic receipts. The complete previous attack matrix remains green.
- Focused tests: 41 passed. Public package validation, generated-project doctor, and the SECOND
  LOOK policy verifier passed. Ruff and strict type checking are clean.
- Guard SHA-256 in all three roots:
  `6a2b8c53560be68fc2aef34129a164a23d70aaa9ec1ef3d217ace9b43105e153`.
- Maker summary:
  `.planning/essential-tasks/2026-08-16-agent-plus-anti-loop-guard-v2-maker-summary.md`.
- Fresh integrated review packet:
  `.planning/essential-tasks/2026-08-16-agent-plus-anti-loop-guard-v2-integrated-review-packet.md`.

Status: `AGENT_PLUS_ANTI_LOOP_GUARD_V2_MAKER_COMPLETE — FRESH INTEGRATED REVIEW REQUIRED`.
The public coverage BLOCK is unchanged. No acceptance or promotion is claimed.

## Anti-Loop Guard v2 — outcome-catalog repair maker complete (2026-08-16)

- The prior fresh v2 review found that a scratch ledger could authorize process-only IDs under a
  valid outcome category. The repair adds an immutable guard-owned `OUTCOME_CATALOG` mapping each
  permitted ID to exactly one kind. A ledger may select only a catalog subset; unknown, renamed,
  case-changed, path-like, and recategorized IDs fail closed.
- Tests cover every named process ID under every promotable kind, catalog forgery and
  recategorization, valid user-visible/scientific/execution outcomes, and packet/ledger subset
  binding. The prior v1/v2 attack matrix remains green.
- Agent+, bootstrap, and SECOND LOOK guard copies are byte-identical at
  `b9519db5171e1e2003bb4d94ab3b600e907e057cd446d9b1ac65a5d78d1c6f8c`. Focused tests: 45 passed;
  public validation, generated-project doctor, policy verifier, Ruff, and strict mypy passed.
- This is maker evidence only. One fresh independent integrated review is required. The public
  coverage BLOCK is unchanged. F-16 rehearsal, Wave B, certified-data reads, modeling, promotion,
  and scientific/live execution remain stopped.

Status: `AGENT_PLUS_ANTI_LOOP_GUARD_V2_OUTCOME_CATALOG_MAKER_COMPLETE — FRESH INTEGRATED REVIEW REQUIRED`.
Next: run one fresh independent review from
`.planning/essential-tasks/2026-08-16-agent-plus-anti-loop-guard-v2-integrated-review-packet.md`.

## Anti-Loop Guard v2 — outcome-catalog integrated review accepted (2026-08-17)

One fresh independent integrated review accepted the shared Guard v2 boundary after 117
reviewer-authored scratch attacks passed with zero failures. The guard-owned immutable
`OUTCOME_CATALOG` rejects all named process-only IDs under every promotable category and rejects
forged, renamed, case-changed, Unicode lookalike, path-like, and recategorized IDs. Valid catalog
entries require both catalog and ledger authorization.

Agent+, bootstrap, and SECOND LOOK guard copies remain byte-identical at
`b9519db5171e1e2003bb4d94ab3b600e907e057cd446d9b1ac65a5d78d1c6f8c`. The focused suite (45),
public validator, generated-project doctor, SECOND LOOK policy verifier, Ruff, and strict mypy
passed. Ruff and mypy used isolated `uvx` environments because neither executable is installed on
PATH.

This closes the Anti-Loop Guard v2 engineering review only. The public coverage BLOCK remains;
F-16 remains OPEN/BLOCKED; no rehearsal, certified-data read, Wave B, modeling, promotion, or
scientific/live execution is authorized. No Git operation occurred.

Status: `AGENT_PLUS_ANTI_LOOP_GUARD_V2_REVIEW_ACCEPTED — ENGINEERING GATE CLOSED`.
Primary record:
`.planning/essential-tasks/2026-08-17-agent-plus-anti-loop-guard-v2-outcome-catalog-integrated-review-record.md`.

## Higher-capacity routing policy — maker complete (2026-08-17)

- Added generic `quick`, `standard`, `deep`, and `scientific/live` routing modes to the public
  policy and bootstrap controls. Extra capacity changes bounded throughput only; authority, context
  ceilings, retries, reviewer count, scientific/live first-error behavior, and human authorization
  remain unchanged.
- Added sequential engineering-chain controls, one-writer and three-worker limits, per-role budgets,
  premium reserve, explicit overnight boundary, review cadence, and the complete task-card fields.
- Owner authorization covers a 10-completed-chain experiment. The local model-review baseline had
  zero traffic, so this is not a measured throughput, quality, or performance claim.
- Guard v2, role separation, two-BLOCK architecture reset, first-error scientific/live behavior, and
  outcome metrics were preserved. No Guard v2 implementation, ledger, or accepted receipt changed.
- Necessity and decision record:
  `.planning/essential-tasks/2026-08-17-agent-plus-higher-capacity-routing-necessity.md`.
- Unit tests, shell syntax, bootstrap initialization/doctor, changed-file safety checks, and the
  full public-package validator passed. The generated model-review report now uses the public-safe
  project label `agent-plus` instead of a private local path.

Status: `AGENT_PLUS_HIGH_CAPACITY_ROUTING_MAKER_COMPLETE — FRESH REVIEW REQUIRED`.
Next: run one fresh independent review of the changed public and bootstrap routing boundary. The
public coverage `BLOCK` and all scientific/live holds remain unchanged.

## Higher-capacity routing — cross-project review BLOCK (2026-08-17)

The Agent+ public and bootstrap policy passed the full public validator. The cross-project review
blocked promotion on one SECOND LOOK hook gap: the `live` authorization scope does not yet force
zero parallel workers. The next bounded correction also must replace a truthy owner-reference
check with an exact typed authority binding. Agent+ public policy itself had no review defect, but
the operating profile is not accepted until the enforcement correction passes one fresh review.

Status: `AGENT_PLUS_HIGH_CAPACITY_ROUTING_REVIEW_BLOCKED — ENFORCEMENT CORRECTION REQUIRED`.

## Higher-capacity enforcement — architecture reset required (2026-08-17)

The bounded SECOND LOOK correction closed live-worker and owner-authority binding, but fresh review
found non-finite reserve values fail open. This is the second BLOCK at the orchestration packet
validator. A third local patch is forbidden. The operating profile remains unaccepted until SECOND
LOOK replaces the open packet dictionary with a closed typed schema and passes one fresh review.

Status: `AGENT_PLUS_HIGH_CAPACITY_ROUTING_BLOCKED — PACKET_ARCHITECTURE_RESET_REQUIRED`.

## Higher-capacity closed packet reset — review BLOCK (2026-08-17)

SECOND LOOK completed the closed packet architecture reset. Its packet, owner, overnight, shell,
and write-tool controls passed fresh review. Promotion remains blocked by one hook-input error:
malformed `Read.limit` values raise raw Python exceptions rather than a typed denial. The public
Agent+ policy remains valid, but the operating profile is not accepted until that bounded hook
input correction receives fresh review.

Status: `AGENT_PLUS_HIGH_CAPACITY_ROUTING_BLOCKED — READ_LIMIT_TYPED_DENIAL_REQUIRED`.

## Higher-capacity routing — ACCEPTED (2026-08-17)

SECOND LOOK closed the final typed `Read.limit` boundary and passed one fresh integrated review:
84 focused tests, strict packet/JSON controls, live zero workers, exact owner authority, review
write denial, and shell-substitution denial. The generic Agent+ public/bootstrap policy had already
passed full package validation.

The higher-capacity engineering profile is accepted as the default. It increases bounded Luna and
Claude throughput without changing context ceilings, attempts, scientific/live authority, or owner
promotion rights.

Status: `AGENT_PLUS_HIGH_CAPACITY_ROUTING_ACCEPTED — DEFAULT PROFILE ACTIVE`.

## Portable Agent+ system — maker complete (2026-08-17)

The Agent+ repository is now the canonical distribution. Version `0.2.0` adds a small lifecycle
interface for initialization, verification, status, safe upgrade, and local project context.
Installed projects record managed-file hashes. Upgrade refuses local managed-file drift and
preserves project instructions, memory, plans, evidence, data, and mutable ledgers.

SECOND LOOK is bound as a proving project through an exact source/version manifest and fail-closed
shared-copy check. Reusable improvements must return through Agent+ with synthetic public evidence.
The public validator passed with 48 tests. SECOND LOOK source-missing, version-mismatch, and
copy-mismatch attacks passed with Ruff, strict mypy, and the policy verifier.

Status: `AGENT_PLUS_PORTABLE_SYSTEM_MAKER_COMPLETE — FRESH REVIEW REQUIRED`.
Next: run one fresh integrated review before commit, push, tag, release, or public announcement.

## Agent+ Discovery Grill — maker complete (2026-08-18)

Matt Pocock's upstream `grill-me` and required `grilling` primitive are installed unchanged in the
local Codex skill library from commit `9c9f36ccd3995266cd675468af71639c8dde1ec5`. The canonical
Agent+ package now contains a separate attributed `agent-plus-discovery-grill` adaptation.

The Agent+ skill asks one question at a time for voice-friendly discovery and updates one
`.planning/discovery/PROJECT-DISCOVERY.md` decision brief after each answer. It stores settled
decisions, unknowns, evidence and prototype needs, the current frontier, and a restart instruction.
It explicitly forbids raw voice/chat transcripts and cannot replace a research contract or grant
analysis, modeling, live, scientific, or promotion authority.

Skill validation passed. The public and local skill folders are byte-equivalent by recursive diff.
The public package passed all 48 tests, canonical/bootstrap guard binding, internal Markdown links,
and public-package validation. No commit, push, tag, release, or public announcement occurred.

Status: `AGENT_PLUS_DISCOVERY_GRILL_MAKER_COMPLETE — FRESH BEHAVIORAL REVIEW REQUIRED`.
Next: run one fresh review of the voice flow, resume behavior, artifact privacy, typed exits, and
research-contract handoff before including the skill in the public Agent+ release.

## Agent+ Discovery Grill integration audit — maker complete (2026-08-18)

The canonical/installed discovery adaptation, original upstream skills, attribution, MIT license,
OpenAI metadata, decision-brief template, router, bootstrap, validator, and curated Brain start
pages were audited as one integration boundary.

Three generic project-start gaps were corrected: the shared router now selects discovery before a
contract only when the decision is unclear; new projects begin at a discovery-or-contract gate; and
the public validator now binds every discovery file plus its router and bootstrap consumers. The
Brain new-project, activation, and Agent Tooling pages now describe the same conditional route.

Public validation passed with 49 tests. Lifecycle tests passed 4/4. Canonical/local discovery copies
remain byte-equivalent. Installed `grill-me`, installed `grilling`, and the bundled license match
upstream commit `9c9f36ccd3995266cd675468af71639c8dde1ec5` and current upstream `main` at audit
time. No SECOND LOOK scientific state, private material, data, Git state, or release state changed.

Status: `AGENT_PLUS_DISCOVERY_INTEGRATION_AUDIT_MAKER_COMPLETE — FRESH BEHAVIORAL REVIEW REQUIRED`.
Next: one fresh behavioral review of the complete discovery and project-start boundary before any
commit, push, tag, release, or announcement.

## Interface-consumer closure — maker complete (2026-08-18)

Agent+ now has a closed, deterministic consumer-closure receipt and validator. A changed interface
cannot be declared ready while a discovered production consumer, runtime wrapper, diagnostic,
launcher, serializer, test seam, or documentation consumer is omitted. Required real-shape smoke
evidence must be PASS. The essential-task protocol and template now require these fields.

The canonical and bootstrap guard copies are byte-identical. Seven focused attacks, 56 Agent+ unit
tests, Ruff, strict mypy, canonical/bootstrap binding, internal links, and the public-package
validator passed. SECOND LOOK contains a proving-project adapter for the attempt-06 checkpoint-key
failure and belongs in the same fresh integrated review.

No release, commit, push, tag, or announcement occurred.

Status: `AGENT_PLUS_INTERFACE_CONSUMER_CLOSURE_MAKER_COMPLETE — FRESH INTEGRATED REVIEW REQUIRED`.
Next: one fresh integrated engineering review of the generic control and SECOND LOOK adapter.

## Interface-consumer closure review correction — maker complete (2026-08-18)

The first integrated review found that the public validator rejected a private absolute path in
Agent+ local memory. The local restart command is now public-safe and relative. The public-package
validator passes again with 56 tests, canonical/bootstrap binding, and internal links. Seven
focused guard tests also pass.

The other review finding was repaired in SECOND LOOK. One new fresh integrated review is required.
No release, commit, push, tag, or announcement occurred.

Status: `AGENT_PLUS_INTERFACE_CONSUMER_CLOSURE_REPAIR_MAKER_COMPLETE — FRESH INTEGRATED REVIEW REQUIRED`.
